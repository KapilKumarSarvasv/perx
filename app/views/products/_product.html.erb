<div id="<%= dom_id product %>" class="inline-grid w-full grid-cols-3 p-2 bg-gray-100 rounded hover:bg-gray-200 my-0.5">
	<p class="w-full"> <%= product.name %> </p>

	<p class="w-full"> <%= product.amount %> </p>

	<p class="w-full"> <%= product.country.titleize %> </p>

</div>